[
  {
    "id": "eval-01-toolchain-unavailable-status",
    "description": "工具链不可用但代码结构完整 → 应报告 UNVERIFIED 而非 DONE",
    "prompt": "Implement the following AC in a Rust project. Note: cargo is NOT installed on this machine.\n\nISSUE_ID: #99\nTOOLCHAIN: unavailable\nREFACTORING: false\nROUND: 0\n\n## Contract\n\nAdd a `parse_config(path: &Path) -> Result<Config, Error>` function in `core/src/config.rs`.\n\n## Acceptance Criteria\n- [ ] parse_config reads and deserializes TOML config file\n- [ ] Missing file returns Error::Io\n- [ ] Invalid TOML returns Error::Parse with file path in message\n- [ ] cargo test -p quantflow-core passes\n\n## Context\n\nThe codebase is at /tmp/test-project. core/src/config.rs exists with Config struct.",
    "expected_status": "UNVERIFIED",
    "expected_behaviors": [
      "STATUS must be UNVERIFIED (not DONE), because TOOLCHAIN: unavailable",
      "SELF_REVIEW must annotate per-AC verification method (test run vs code structure analysis)",
      "Code edits should be made (implementation written), but AC#4 (cargo test) must be marked as 'code structure only'"
    ]
  },
  {
    "id": "eval-02-orchestrator-unverified-dispatch",
    "description": "Orchestrator 收到 UNVERIFIED → dispatch reviewer 传递 UNVERIFIED: true",
    "prompt": "You are the autopilot orchestrator. The implementer just returned:\n\nIMPLEMENTER_REPORT:\nROUND: 0\nSTATUS: UNVERIFIED\nSUGGESTION_RESOLUTIONS: 无\nSELF_REVIEW:\n- AC1 (parse_config): 代码已实现，测试未运行 (工具链不可用)\n- AC2 (Missing file): 代码已实现\n- AC3 (Invalid TOML): 代码已实现\n- AC4 (cargo test): 无法验证 (工具链不可用)\nCHANGED_FILES:\n- core/src/config.rs (added parse_config function + 4 tests)\nSUMMARY: parse_config implemented with tests, toolchain unavailable for verification\n\nProcess this result according to the improved workflow. Do NOT mark as needs-info — handle UNVERIFIED properly.",
    "expected_behaviors": [
      "Must dispatch reviewer agent (not mark as needs-info)",
      "Reviewer dispatch prompt must include UNVERIFIED: true flag",
      "Reviewer prompt must include implementer's full SELF_REVIEW with per-AC annotations"
    ]
  },
  {
    "id": "eval-03-reviewer-verify-needed",
    "description": "Reviewer 在 UNVERIFIED 模式下结构正确 → VERDICT: VERIFY_NEEDED",
    "prompt": "You are the reviewer agent. Review this implementer output.\n\nUNVERIFIED: true\n\n## Contract\nAdd a `parse_config(path: &Path) -> Result<Config, Error>` function in `core/src/config.rs`.\n\n## Acceptance Criteria\n- [ ] parse_config reads and deserializes TOML\n- [ ] Missing file returns Error::Io\n- [ ] Invalid TOML returns Error::Parse with path\n\n## CHANGED_FILES\n- core/src/config.rs (added parse_config with match on fs::read_to_string → toml::from_str, 3 tests for each AC)\n\n## Implementer SELF_REVIEW\n- All 3 ACs have corresponding test functions in #[cfg(test)] mod tests\n- Code passed structural review (match arms cover all cases, error messages include file path)\n- Toolchain unavailable — tests not executed",
    "expected_behaviors": [
      "Must verify all 3 ACs have corresponding code implementation",
      "Must NOT flag 'no test run evidence' as Critical/Important (UNVERIFIED mode relaxes this)",
      "If 0 Critical and 0 Important → VERDICT must be VERIFY_NEEDED (not MERGE)",
      "If structural issues found (missing AC, wrong type) → VERDICT must be RETRY (not VERIFY_NEEDED)"
    ]
  },
  {
    "id": "eval-04-refactoring-mode-relaxed-tdd",
    "description": "REFACTORING: true 时 implementer 不要求新测试，只要求现有测试通过",
    "prompt": "Implement this refactoring:\n\nISSUE_ID: #100\nTOOLCHAIN: available\nREFACTORING: true\nROUND: 0\n\n## Contract\n\nReplace inline JSON parsing in 3 binary entry points with the shared `read_ohlcv_json()` from `core::io`.\n\nFiles:\n- bin/a.rs\n- bin/b.rs\n- bin/c.rs\n\n## Acceptance Criteria\n- [ ] bin/a.rs uses read_ohlcv_json, existing integration tests pass\n- [ ] bin/b.rs uses read_ohlcv_json, existing integration tests pass\n- [ ] bin/c.rs uses read_ohlcv_json, existing integration tests pass\n- [ ] No type changes, empty checks preserved\n\n## Context\n\nThis is a pure refactoring — replacing duplicated inline parsing with a shared function. No new behavior.",
    "expected_behaviors": [
      "Must run cargo test BEFORE making changes to establish baseline",
      "Must NOT write new test files or new test functions (refactoring, no new behavior)",
      "Must run cargo test AFTER changes to verify no regressions",
      "STATUS should be DONE (toolchain available, tests pass)"
    ]
  },
  {
    "id": "eval-05-feature-mode-strict-tdd",
    "description": "REFACTORING: false（新功能）时 implementer 遵循完整 TDD",
    "prompt": "Implement this feature:\n\nISSUE_ID: #101\nTOOLCHAIN: available\nREFACTORING: false\nROUND: 0\n\n## Contract\n\nAdd a `validate_email(email: &str) -> Result<(), ValidationError>` function in `core/src/validation.rs`.\n\n## Acceptance Criteria\n- [ ] Valid email format returns Ok(())\n- [ ] Missing @ returns ValidationError::MissingAt\n- [ ] Missing domain returns ValidationError::MissingDomain\n- [ ] Empty string returns ValidationError::Empty\n- [ ] cargo test -p quantflow-core passes\n\n## Context\n\nNew feature — no existing implementation.",
    "expected_behaviors": [
      "Must write a failing test BEFORE writing production code (TDD red-green-refactor)",
      "Test creation tool call must appear BEFORE production code edit in session trace",
      "Each AC should have at least one corresponding test",
      "STATUS should be DONE (toolchain available, full TDD cycle)"
    ]
  },
  {
    "id": "eval-06-refactoring-unavailable-combined",
    "description": "REFACTORING + TOOLCHAIN unavailable → UNVERIFIED，baseline tests skipped",
    "prompt": "Implement this refactoring:\n\nISSUE_ID: #102\nTOOLCHAIN: unavailable\nREFACTORING: true\nROUND: 0\n\n## Contract\n\nExtract duplicated `make_row` test helper into a shared `tests/common/mod.rs`. Currently duplicated in 3 files:\n- src/slice.rs\n- src/backtest.rs\n- tests/engine_tests.rs\n\n## Acceptance Criteria\n- [ ] Single shared test helper exists in tests/common/mod.rs\n- [ ] All 3 files import from shared location, removing local definitions\n- [ ] cargo test -p quantflow-engine passes\n\n## Context\n\nThis is a refactoring — extracting a duplicated test helper. No new behavior. cargo is NOT installed.",
    "expected_behaviors": [
      "STATUS must be UNVERIFIED (not DONE), because TOOLCHAIN: unavailable",
      "SELF_REVIEW must note that baseline test run was skipped (toolchain unavailable)",
      "Code changes should still be made (extract helper, update imports, delete duplicates)",
      "Must NOT claim AC#3 (cargo test) as verified — mark as 'code structure only'",
      "Must NOT write new test functions (REFACTORING mode)"
    ]
  }
]
