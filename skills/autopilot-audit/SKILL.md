---
name: autopilot-audit
description: Post-hoc audit of autopilot execution fidelity. Analyzes session traces from an /autopilot run to evaluate how faithfully the workflow executed against its contract, surfacing errors, friction, and drift with traceable evidence anchors.
---

Load the `audit-autopilot` skill and execute this request:

Audit the autopilot execution with orchestrator session {{sessionId}}.
