[project]
name = "skill-creator"
version = "0.1.0"
description = "Scripts for creating, testing, and improving agent skills"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
